[project]
name = "fastembed-service"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "fastembed>=0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
