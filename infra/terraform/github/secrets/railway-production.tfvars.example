# infra/terraform/github/secrets/railway-production.tfvars.example  (TRACKED — keys only)
#
# Copy to  secrets/railway-production.tfvars  (git-ignored). Overlaid AFTER
# railway-shared.tfvars. Railway service env-var values that differ for production.
# Pushed by scripts/push_railway_variables.py; NOT read by Terraform.
#
# In CI: == GitHub Actions secret TF_TFVARS_RAILWAY_PRODUCTION (base64).
# ---------------------------------------------------------------------------

# Auth signing key (HS256) — dashboard_frontend + dashboard_backend NEXTAUTH_SECRET.
# MUST differ from staging's. Generate with `openssl rand -base64 32`.
nextauth_secret = ""

# weekly-report R2 bucket differs per environment
r2_bucket_name = ""
r2_public_url  = ""
