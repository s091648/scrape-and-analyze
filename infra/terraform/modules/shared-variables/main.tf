# Intentionally no resources here — this module only groups & reshapes inputs
# into outputs.tf's named maps (see that file's header comment). It exists
# purely as the Terraform equivalent of a central "constants" file that
# multiple stacks import from, for values genuinely identical across several
# services in the same environment.
