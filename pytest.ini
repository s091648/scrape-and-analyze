[pytest]
testpaths =
    src/tests
    backend/tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
pythonpath = .
markers =
    integration: marks tests as integration tests (require running database)
